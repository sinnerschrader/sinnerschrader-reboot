<!DOCTYPE html>
<html lang="en">
  {% include head %}
  <body class="{{bodyClass}}">
    {% include navigation, data: global %}
    <h2>{{ title }}</h2>
    {{ content }}
    {% include footer, data: global %}
  </body>
</html>
